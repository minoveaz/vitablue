/**
 * @deprecated Import `BackofficeShell` from `@/components/layouts/BackofficeShell`.
 * The layout adapter is the canonical compatibility boundary over SuiteShell
 * and SuiteCanvas. This alias remains to avoid breaking legacy imports.
 */
export { default } from '../layouts/BackofficeShell';
